"""The reachability census: the vendored engine consumption manifest, the
walk that finds every field a root reaches, and the disposition of every
field the engine does not read. See ``census.consumption.disposition``.
"""
