{
  "$defs": {
    "DocumentSet": {
      "additionalProperties": false,
      "description": "Authored documents keyed by relative path, each value the document's\nfile text. The request carrying the set names what a path is relative to.\n\nA key is a relative POSIX path with exactly one spelling per\ndocument; the key pattern carries the grammar. A key that names a\ndocument may not also be an ancestor directory of another key. The text\nis opaque to this model.",
      "maxProperties": 2000,
      "patternProperties": {
        "^(?:[^/.\\x00][^/\\x00]*|\\.[^/.\\x00][^/\\x00]*|\\.\\.[^/\\x00]+)(?:/(?:[^/.\\x00][^/\\x00]*|\\.[^/.\\x00][^/\\x00]*|\\.\\.[^/\\x00]+))*(?![\\s\\S])": {
          "maxLength": 500000,
          "type": "string"
        }
      },
      "propertyNames": {
        "maxLength": 1024
      },
      "title": "DocumentSet",
      "type": "object"
    }
  },
  "$id": "https://schemas.analitiq.ai/validate-package-request/4.0.0.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "allOf": [
    {
      "if": {
        "properties": {
          "package_kind": {
            "const": "connection"
          }
        }
      },
      "then": {
        "properties": {
          "documents": {
            "propertyNames": {
              "not": {
                "anyOf": [
                  {
                    "pattern": "^\\.secrets/credentials\\.json(?![\\s\\S])"
                  }
                ]
              }
            }
          }
        }
      }
    }
  ],
  "description": "Public JSON Schema contract for a request to validate one package, named by the kind of its root document and supplied as its documents: each document's file text keyed by its relative path in the package. The schema gates the request's shape only; the documents' content is not judged by it. Source of truth: analitiq.contracts.validation_requests.ValidatePackageRequest (Pydantic).",
  "properties": {
    "documents": {
      "$ref": "#/$defs/DocumentSet"
    },
    "package_kind": {
      "description": "The kind of the package the documents form, which is the kind of its root document.",
      "enum": [
        "connector",
        "connection",
        "pipeline"
      ],
      "title": "Package Kind",
      "type": "string"
    }
  },
  "required": [
    "package_kind",
    "documents"
  ],
  "title": "Analitiq Validate Package Request",
  "type": "object",
  "version": "4.0.0"
}
