{
  "$defs": {
    "DocumentSet": {
      "additionalProperties": false,
      "description": "Authored documents keyed by the relative path each occupies in its\npackage, each value the document's file text.\n\nA key is a package-relative POSIX path with exactly one spelling per\ndocument; the key pattern carries the grammar. A key that names a\ndocument may not also be the directory of another key. The text is\nopaque to this model.",
      "maxProperties": 2000,
      "patternProperties": {
        "^(?:[^/.][^/]*|\\.[^/.][^/]*|\\.\\.[^/]+)(?:/(?:[^/.][^/]*|\\.[^/.][^/]*|\\.\\.[^/]+))*(?![\\s\\S])": {
          "maxLength": 500000,
          "type": "string"
        }
      },
      "propertyNames": {
        "maxLength": 1024
      },
      "title": "DocumentSet",
      "type": "object"
    }
  },
  "$id": "https://schemas.analitiq.ai/validate-package-request/1.0.0.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Public JSON Schema contract for a request to validate one connector or pipeline package supplied as its documents: each document's file text keyed by its relative path in the package. The schema gates the request's shape only; the documents' content is not judged by it. Source of truth: analitiq.contracts.validation_requests.ValidatePackageRequest (Pydantic).",
  "properties": {
    "documents": {
      "$ref": "#/$defs/DocumentSet"
    }
  },
  "required": [
    "documents"
  ],
  "title": "Analitiq Validate Package Request",
  "type": "object",
  "version": "1.0.0"
}
