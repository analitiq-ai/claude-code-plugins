{
  "$defs": {
    "DocumentSet": {
      "additionalProperties": false,
      "description": "Authored documents keyed by the relative path each occupies in its\npackage, each value the document's file text.\n\nA key is a package-relative POSIX path with exactly one spelling per\ndocument; the key pattern carries the grammar. A key that names a\ndocument may not also be an ancestor directory of another key. The text\nis opaque to this model.",
      "maxProperties": 2000,
      "patternProperties": {
        "^(?:[^/.\\x00][^/\\x00]*|\\.[^/.\\x00][^/\\x00]*|\\.\\.[^/\\x00]+)(?:/(?:[^/.\\x00][^/\\x00]*|\\.[^/.\\x00][^/\\x00]*|\\.\\.[^/\\x00]+))*(?![\\s\\S])": {
          "maxLength": 500000,
          "type": "string"
        }
      },
      "propertyNames": {
        "maxLength": 1024
      },
      "title": "DocumentSet",
      "type": "object"
    }
  },
  "$id": "https://schemas.analitiq.ai/validate-package-request/3.0.0.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "allOf": [
    {
      "if": {
        "properties": {
          "package": {
            "const": "connection-package"
          }
        }
      },
      "then": {
        "properties": {
          "documents": {
            "propertyNames": {
              "not": {
                "anyOf": [
                  {
                    "pattern": "^\\.secrets/credentials\\.json(?![\\s\\S])"
                  }
                ]
              }
            }
          }
        }
      }
    }
  ],
  "description": "Public JSON Schema contract for a request to validate one package, named by its published package schema and supplied as its documents: each document's file text keyed by its relative path in the package. The schema gates the request's shape only; the documents' content is not judged by it. Source of truth: analitiq.contracts.validation_requests.ValidatePackageRequest (Pydantic).",
  "properties": {
    "documents": {
      "$ref": "#/$defs/DocumentSet"
    },
    "package": {
      "description": "Name of the published package schema the documents form.",
      "enum": [
        "connector-package",
        "connection-package",
        "pipeline-package"
      ],
      "title": "Package",
      "type": "string"
    }
  },
  "required": [
    "package",
    "documents"
  ],
  "title": "Analitiq Validate Package Request",
  "type": "object",
  "version": "3.0.0"
}
