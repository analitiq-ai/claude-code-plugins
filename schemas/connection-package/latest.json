{
  "$id": "https://schemas.analitiq.ai/connection-package/latest.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Public JSON Schema contract for the authored documents of a connection package: where each sits, read from the connection's own directory (the one holding `connection.json`), and the published schema it is written against. It holds locations only; each document's shape is the schema it points at, and a location marked `x-secret` holds secret values. Other files a package carries are outside this schema. Source of truth: analitiq.contracts.connection_package.ConnectionPackage (Pydantic).",
  "patternProperties": {
    "^\\.secrets/credentials\\.json(?![\\s\\S])": {
      "$ref": "https://schemas.analitiq.ai/credentials/latest.json",
      "x-secret": true
    },
    "^connection\\.json(?![\\s\\S])": {
      "$ref": "https://schemas.analitiq.ai/connection/latest.json"
    },
    "^definition/endpoints/[^/]+\\.json(?![\\s\\S])": {
      "$ref": "https://schemas.analitiq.ai/database-endpoint/latest.json"
    },
    "^definition/type-map\\.json(?![\\s\\S])": {
      "$ref": "https://schemas.analitiq.ai/type-map/latest.json"
    }
  },
  "required": [
    "connection.json"
  ],
  "title": "Analitiq Connection Package",
  "type": "object",
  "version": "2.1.0"
}
