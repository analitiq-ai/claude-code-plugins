.DS_Store
*.log
node_modules/
__pycache__/
*.pyc
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
.idea/
# Claude Code's working directory: local settings, session state, and
# `worktrees/`, which holds whole nested checkouts. `skills/` stays out too —
# it is the maintainer's own tooling, assembled per machine and partly
# symlinked out to other repos, so tracking it would publish paths nobody
# outside can follow; whatever is re-included below is the exception.
.claude/*
# `rules/` is the exception: authoring policy this repo's prose and tests cite
# by path. Ignored, those citations resolved only on the machine that wrote
# them; tracked, they resolve for anyone with a clone.
!.claude/rules/
# `skills/rules-audit/` is tracked for the same reason: it is the audit half of
# `rules/`, deriving its triggers from those files' `paths:` frontmatter or its
# absence, and it must reach anyone who can open a PR. The rest of `skills/`
# stays local.
!.claude/skills/
.claude/skills/*
!.claude/skills/rules-audit/
# `settings.json` is tracked because it carries shared project configuration:
# the render hook that regenerates derived artifacts after a session edits a
# generator source, and the plugin enablement contributors share. Personal
# overrides go in settings.local.json, which stays ignored.
!.claude/settings.json

# Python packaging build artifacts
build/
dist/
*.egg-info/
*.egg

# Design docs kept local — not published to the public repo for now
docs/

# One spec per issue, written into the worktree the issue is implemented in. It
# is the authority while the work is open and goes with the worktree when the
# work merges; the decisions survive as a comment on the issue.
specs/

# Personal instructions to Claude Code, the prose counterpart of
# settings.local.json. Shared project instruction lives in CLAUDE.md.
CLAUDE.local.md

# Local env / direnv — may hold secrets (GitHub PATs, etc.), never commit
.envrc
.env
.env.*

# Local MCP server config — machine-specific tooling, not project contract.
.mcp.json


# Eval run log — one JSON line per finished run, appended locally by
# evals/run_evals.py. A measurement of a moment, not a tracked artifact.
eval-results.jsonl
